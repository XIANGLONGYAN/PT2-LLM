# Core
# NOTE: transformers>=4.43 is required (the quantization/eval code passes
# `position_embeddings` to the decoder layers, an API added in the 4.43 refactor).
torch==2.4.0
transformers==4.44.2
tokenizers==0.19.1
accelerate==0.32.1
datasets==2.19.2
sentencepiece==0.2.1
numpy==1.26.4
protobuf==4.25.1
scikit-learn
loguru

# Utilities used by the codebase
exceptiongroup
pyparsing

# Evaluation (bundled under ./lm-evaluation-harness, install with `pip install -e`)
# lm-eval
